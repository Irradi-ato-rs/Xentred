name: RER Free Fallback Rollout (v0.9)

on:
  workflow_dispatch:
    inputs:
      org: Irradi-ato-rs
        required: true
      default_branch:
        default: main
      repos_allowlist:
        required: false
      dry_run:
        default: "true"
      export_evidence:
        default: "false"
      strict:
        default: "0"

jobs:
  rollout:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      ORG: ${{ inputs.org }}
      DEFAULT_BRANCH: ${{ inputs.default_branch }}
      REPOS_ALLOWLIST: ${{ inputs.repos_allowlist }}
      DRY_RUN: ${{ inputs.dry_run }}
      EXPORT_EVIDENCE: ${{ inputs.export_evidence }}
      STRICT: ${{ inputs.strict }}
      GH_TOKEN: ${{ secrets.ORG_ADMIN_TOKEN }}

    steps:
      - name: Checkout (pinned)
        uses: actions/checkout@8ade135a41bc03ea155e62e844d188df1ea18608
        # ^ checkout v4.1.5 commit SHA (example — verify and freeze)

      - name: Run RER Free Fallback Rollout
        shell: bash
        run: |
          bash .github/scripts/rer-free-fallback-rollout.sh